# 0006 — The work-issue phasing-flow rewire: a SKILL-rewire driving the existing seeds (no new kernel), Mode-B Phase-4 engine-gating, S/M/L-conditional host, Phase-3.5 deletion, a 10-block frozen-region guard, and the ADR-0004 fixed-point discharge

- **Status:** accepted
- **Date:** 2026-06-06
- **Decision:** M3 adoption (skill 2 of 3, the deepest rebuild) re-platforms `work-issue` onto the phasing-flow framework **internally**, preserving the `/work-issue N` 8-phase issue→PR consumer contract byte-for-behavior. Unlike the integrate-branch audit seed (ADR 0005), work-issue ships **NO new compute kernel / bundle / verdict-parity harness** — it has no parity-lockable arithmetic (scope S/M/L is a soft judgment, `SKILL.md:217`; the Phase-3.5 detector is being *deleted*; the `steps→units` slicer is framework-owned, `phasing-flow/SKILL.md:41,61,96`). Twelve sub-decisions, all resolved by the human at the GATE-2 plan gate (planning-brain run `wf_dacd4c66-a44`, whose adversarial critic forced material corrections): **(a) OD3 = a SKILL REWIRE** that drives the already-live `planning-brain` + `phasing-flow-engine` seeds **by scriptPath** — two additive `### Run via the committed seed (M3)` pointer blocks, no `workflow/` dir; **(b) OD3-host:** the no-regression frozen-region test needs a CI host — the claimed "fold into structural-validation" is a **phantom** (that job has zero `node` steps; every `node --test` job is seed-scoped), so a dedicated **`work-issue-frozen-harness`** job is added and **CI goes 19→20** (the honest M3.1 mirror; the "19/19 stays free" headline does **not** survive); **(c) OD1 = engine-host is S/M/L-CONDITIONAL** — the Phase-4 execution-engine gate runs for M/L issues; small issues keep the in-session TDD loop (the 1M window neutralizes the context-relief motive on small issues; the deterministic fail-CLOSED verify happens at Phase 5 either way); **(d) OD4 = Mode B** — the engine is the per-AC **verify + adversarial diff-review GATE**, with RED→GREEN→REFACTOR authoring kept **in-session** (via `superpowers:test-driven-development`, the enforcer the engine lacks), one AC = one unit, serial — because the engine executor is a *generic* agent and a deterministic verify cannot prove a test was RED before the implementation, so encoding the whole TDD body in `unit.instruction` (Mode A) would risk GREEN-without-RED that no verify catches; **(e) OD2 = orchestrator-TDD, N/A-for-shape** (a Markdown rewire + one test + one CI job has no dispatchable units; the engine auto-verify is inert for the kit); **(f) DELETE Phase 3.5** (D-A: a real divergence from impact-matrix:66's "re-point at /workflow" — DELETE, not re-point) + scrub its 4 ripple sites (the `L468-585` block, the `--phase`/`--no-phase` flags, the count line, the Phase-4 "if phasing was invoked" note); **(g) ADD the planning-brain at Phase 3** (D-B: the matrix never named it for work-issue) with a **mandatory null-guard** on `result.master_plan` (the structured-output pitfall) → in-session prose fallback, never advancing to Phase 4 without an approved plan; **(h) KEEP Phase 5.5** (e2e/visual) as a HUMAN-GATED orchestrator check (D-D), **never** the engine's fail-OPEN diff-review layer (design.md:62: a running Workflow takes no human input mid-run); **(i) REMOVE the `--phase`/`--no-phase` flags** — a blessed user-facing contract delta (a flag dangling at a deleted phase, or repurposed as an engine toggle, would re-introduce the opt-in escalation layer the framework exists to kill); **(j) Theme-4:** KEEP master-protection NON-NEGOTIABLE and DEFER native worktrees (likely NO-OP, M3.1's precedent) — reject impact-matrix:55-57's "drop main-on-master" policy; **(k)** discharge ADR-0004's deferred **fixed-point** obligation: the dead `fixedPointK` pass-through is **removed** from the engine `wrapper.mjs` (the kernel never destructured it), and the stall guard is recorded as **CONDITIONAL** on a future parallel/retry cursor — keyed on a collision-free `id` (NOT `(id,title)`, the duplicate-label landmine) and RED-testable when it binds; **(l)** the genuinely-untouched SKILL blocks are frozen by a **10-block content-anchored guard** (stronger than the planned 7 — worktrees deferred ⇒ Phase 2 is also frozen). The **REAL D-E** is a no-op alignment note (`/goal verify` aligns with the verification spine, R5:63 — all three scouts mislabeled the flag question as D-E). `critVotes` is **not** recorded — it is a seed-glue parameter; a rewire driving the existing engine inherits the engine's committed value and chooses nothing.
- **Context:** M3 wires `work-issue` / `spec-it` / `integrate-branch` onto the framework à-la-carte by primitive; work-issue is second (after integrate-branch built the `resolveVerifier` + consensus plumbing it inherits) and is the deepest rebuild — it has a TDD-loop body, not a one-shot fan-out. The work was dogfooded `/phasing-flow plan`-style: a 6-reader **Understand** workflow (`wf_50353a0c-83d`) produced a file:line-anchored facts brief, then the **planning-brain** (`wf_dacd4c66-a44`) synthesized a P0..P8 master plan whose **adversarial critic** corrected all three scout proposals on three load-bearing points re-verified against the tree: **(1)** the frozen-test "host" they named (`structural-validation`) has **zero node steps** — every `node --test` job is seed-scoped, so a dedicated 20th harness is required and "CI stays 19/19" is false; **(2)** Mode A-vs-B is a **design** decision forced by the engine being a non-enforcer, **not** empirically probe-decidable (a deterministic verify cannot prove RED-first); **(3)** the front-loaded probe must include a **no-test-script fixture** or it cannot exercise the silent-pass failure mode it guards. The load-bearing assumption (Mode-B wirability) was then confirmed by a **front-loaded live probe** *before* the rewire — mirroring M3.1's `wf_8bf0957a-0cc`.
- **Consequences:**
  - `work-issue/SKILL.md` rewired in four regions only: the invocation preamble (flags removed + a migration note), Phase 3 (planning-brain pointer, M/L, null-guarded), Phase 4 (execution-engine pointer, Mode B, S/M/L conditional, the 4 verify-spine gaps documented as POST-engine orchestrator duties), and the **deletion** of Phase 3.5. Everything else is byte-unchanged.
  - New `skills/work-issue/test/frozen-regions.test.mjs` + `test/golden/{01..10}.md` (10 captured blocks, 31,335 bytes): **13 zero-dep `node:test` cases** (10 byte-identity + 2 pointers-landed + 1 Phase-3.5-deleted/flags-gone), red-capable (a drifted golden turns it red). A dedicated **`work-issue-frozen-harness`** CI job (`plugin-install-fixture.yml`) runs it + a RED gate-the-gate (perturb a golden ⇒ the suite must fail). **CI: 19 → 20 jobs.** No skill/command count ripple (a `test/` dir is not a skill dir; structural-validation counts `-maxdepth 1` skill dirs).
  - The engine `wrapper.mjs` loses the dead `fixedPointK` param (jsdoc + destructure + forward); the committed `phasing-flow-engine.workflow.mjs` bundle is regenerated; the engine's 35 `node:test` cases + bundle parity + check-bundle + import-free all stay green. (ADR-0004 deferred-item discharge.)
  - **Live evidence (the dogfood, this session):** the deterministic `resolveVerifier` probe (with-test → `[npm test --silent, npm run build]`; no-script → `[]`, the silent-pass risk) + two live engine runs — `wf_45463258-703` (a one-AC unit on a Next.js fixture → `clean`, 1/1) and `wf_edb0c262-86b` (a RED verify → `criticals-pending-gate`, 0/1, the `unitsCompleted+unitsResidual===unitsTotal` invariant intact) — confirm Mode B is wirable; the planning-brain run `wf_dacd4c66-a44` returned a non-null `master_plan`. Both seams the rewire points at are live-proven.
  - **Transitive engine-backing of integrate-branch salvage** is now real but **CONDITIONAL (M/L only)** — a small-branch salvage still runs in-session TDD. The PR does not over-claim absolute end-to-end engine-backed salvage.
- **Alternatives considered:**
  - **Build a new work-issue compute seed (OD3=(a)).** Rejected — no parity-lockable arithmetic exists; it would add a full 20th-harness RIG (copied-lib `cmp`, bundle parity, RED gate-the-gate of a kernel) for nothing. The own-seed CI contingency was pre-registered so the P0 choice could not silently invalidate the plan.
  - **"CI stays 19/19" (fold the frozen test into an existing job).** Rejected — verified phantom; `structural-validation` has no node step and every `node --test` job is seed-scoped. A dedicated job → 20/20 is the honest mirror.
  - **Mode A (encode the whole TDD body in `unit.instruction`).** Rejected — the engine is a generic agent and its deterministic verify cannot prove RED-first; Mode A silently abandons work-issue's RED-Green-Refactor + "GREEN fails 3× → stop" contract.
  - **Engine-host for ALL issues (unconditional).** Rejected — engine overhead on trivial issues buys nothing; the 1M window already neutralizes the context-relief motive at small scope.
  - **Re-point Phase 3.5 at `/workflow` (impact-matrix:66) / keep the `--phase` flags.** Rejected — a toggle re-introduces the opt-in escalation layer the framework exists to kill; DELETE is cleaner and the migration note covers discoverability.
  - **Drop "main always on master" (impact-matrix:55-57 Theme-4).** Rejected — master-protection is a NON-NEGOTIABLE multi-terminal safety invariant; adopt the worktree *mechanism* only if it provably enforces it, never the policy-drop.
- **Supersedes / superseded by:** none. Builds on **ADR 0003** (the committed-seed scriptPath contract — work-issue *drives* the seeds rather than adding a fifth), **ADR 0002** (consensus ≥2 — inherited from the engine, not re-tuned), **ADR 0004** (the execution engine — this discharges its deferred `fixed-point`/`fixedPointK` item), and **ADR 0005** (the M3.1 integrate-branch precedent whose additive/revert-clean + frozen-region patterns this mirrors; integrate-branch salvage becomes transitively engine-backed for M/L once this lands).
