# Spec NN — <Title>

<!--
  Replace NN with the next available number after scanning existing specs/NN-*.md files.
  Title should be concise and identify the topic uniquely.
-->

> Status: Draft / awaiting review
> Depends on: <list of prerequisite specs, e.g. "Spec 01"; or "none">
> Related: <other relevant specs that touch overlapping concerns>

## Purpose

<!--
  Why this spec exists. 2–4 sentences. What is being added or changed in the kit
  and why it matters. Avoid implementation language — describe the outcome.
-->

## Sourcing mode

<!--
  Required ONLY when external content is being lifted into the kit.
  One of:
    - verbatim — copied as-is, attribution intact
    - verbatim-with-light-edit — copied with placeholders or minor formatting only
    - customization — derived from external content but materially adapted
    - pattern-only — original work informed by an external pattern

  If this spec is original work and no external content is involved, write:
  "Not applicable — original work."
-->

## <Per-spec content section 1>

<!--
  The body of the spec. Multiple sections as needed.
  Examples:
    - For a templates spec: list of templates with sourcing mode per template
    - For a hooks spec: each hook's purpose, install path, settings registration, manual test recipe
    - For a script spec: behavior, flags, fixtures, edge cases

  Stay focused: this is a structural/pre-implementation spec. Reviewers should
  be able to read it in one pass and approve. Implementation detail lives in
  the implementation, not the spec.
-->

## <Per-spec content section 2>

## Decisions needed

<!--
  Open questions that block approval. List each with the options under consideration
  and the recommended default.

  Format:
    1. **<Question>?** Options: <A> | <B> | <C>. Recommended default: <D>.

  If no decisions remain open, write:
  "None — all decisions resolved during draft."
-->

## Out of scope

<!--
  What this spec deliberately does NOT cover. Useful to prevent scope creep and
  document non-goals.
-->

## Acceptance criteria

<!--
  Checkbox list — testable conditions for "this spec's implementation is complete".
  Each AC must be a verifiable statement.

  - [ ] <criterion>
  - [ ] <criterion>
-->

## Revisions

<!--
  Footer recording revision history. Format:
    - YYYY-MM-DD revision N: <summary>. Addresses: R-NNN, R-NNN.

  Initial draft entry:
    - YYYY-MM-DD revision 1: Initial draft.
-->
